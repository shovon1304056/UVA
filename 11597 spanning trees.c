#include<stdio.h>
int main()
{
    int i=1,a,c;
    while(scanf("%d",&a)==1)
    {
            if(a==0)
              {
              break;}
 printf("Case %d: %d\n",i,a/2);
 i++;
        }
    return 0;
}
